[project]
name = "dm-data-unit-uploader-v2"
version = "0.1.0"
description = "dm-data-unit-uploader-v2 plugin"
requires-python = ">=3.10"
dependencies = [
    "synapse-sdk",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "ruff>=0.8",
]
